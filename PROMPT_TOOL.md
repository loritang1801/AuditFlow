# AuditFlow Prompt And Tooling

## 提示词装配

提示词装配由共享层 `PromptAssemblyService` 完成，AuditFlow 产品层主要提供上下文来源，而不是自行实现一整套提示词引擎。

当前提示词上下文主要来自：

- 周期与控制项元数据
- 历史证据命中结果
- 审阅人评审后沉淀的记忆记录
- 工作区新鲜度与快照相关信息

## 产品工具适配器

`src/auditflow_app/tool_adapters.py` 当前把以下能力注册到共享 `ToolExecutor`：

- 证据搜索
- 工件读取
- 控制项查询
- 评审历史读取
- 映射候选读取
- 快照读取与校验
- 导出准备校验

## 设计原则

- 工具调用必须带组织/工作区上下文，避免跨租户读取
- 提示词使用真实仓储数据，不再依赖固定 demo 占位符
- 模型响应与嵌入都允许配置成远端 provider，也允许本地回退

## 当前实现状态

- Mapper / Skeptic 场景已经使用历史证据与记忆记录做 grounding
- 工具调用有独立审计记录，可按周期、映射或全局查询
- 语义检索可以作为提示词 grounding 的召回来源之一
